package utilities;

import day40_FinalKeyword.ProtectedAccessModifier;

public class AccessModifiersTest3 {
    public static void main(String[] args) {

       // System.out.println(ProtectedAccessModifier.name1); //default is not visible outside the package
       // System.out.println((ProtectedAccessModifier.name2); //protected is not always visiblel outside the package. if it was subclass it would be visible

    }
}
